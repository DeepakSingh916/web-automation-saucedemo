╔════════════════════════════════════════════════════════════════════════════════════╗
║                    PYTEST QUICK COMMAND CHEAT SHEET                                ║
║                  SauceDemo Web Automation Framework                                ║
╚════════════════════════════════════════════════════════════════════════════════════╝

📌 PREREQUISITE: venv\Scripts\activate

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ EXECUTION MODES                                                                     │
├─────────────────────────────────────────────────────────────────────────────────────┤
│ Normal Mode (Browser Visible):   NO FLAG NEEDED                                    │
│ Headless Mode (Browser Hidden):  --headless                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ REPORT BEHAVIOR (Configured in pytest.ini)                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│ HTML Report:        ALWAYS generates at reports/report.html                         │
│ Allure Results:     ALWAYS generates at reports/allure-results/                     │
│ Custom HTML Name:   --html=reports/custom_name.html --self-contained-html           │
│ Custom Allure:      --alluredir=allure-results --clean-alluredir                    │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 1️⃣  RUN ALL TESTS                                                                  │
└─────────────────────────────────────────────────────────────────────────────────────┘

Normal Mode (Browser Visible):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v

Headless Mode (Browser Hidden):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --headless

With Custom Report Names (Normal):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --html=reports/all_tests.html --self-contained-html --alluredir=allure-results --clean-alluredir

With Custom Report Names (Headless):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --headless --html=reports/all_tests.html --self-contained-html --alluredir=allure-results --clean-alluredir


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 2️⃣  RUN SPECIFIC TEST FILE                                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

Normal Mode:
━━━━━━━━━━━━
pytest tests/test_login.py -v
pytest tests/test_products.py -v
pytest tests/test_cart.py -v
pytest tests/test_checkout.py -v

Headless Mode:
━━━━━━━━━━━━━
pytest tests/test_login.py -v --headless
pytest tests/test_products.py -v --headless

With Custom Reports (Normal):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py -v --html=reports/login.html --self-contained-html

With Custom Reports (Headless):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py -v --headless --html=reports/login.html --self-contained-html


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 3️⃣  RUN SPECIFIC TEST CLASS                                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘

Normal Mode:
━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin -v
pytest tests/test_products.py::TestProducts -v
pytest tests/test_cart.py::TestCart -v
pytest tests/test_checkout.py::TestCheckout -v

Headless Mode:
━━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin -v --headless
pytest tests/test_products.py::TestProducts -v --headless


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 4️⃣  RUN SPECIFIC TEST CASE                                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

Normal Mode Examples:
━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin::test_valid_login -v
pytest tests/test_login.py::TestLogin::test_invalid_username -v
pytest tests/test_login.py::TestLogin::test_locked_user -v
pytest tests/test_products.py::TestProducts::test_add_product_to_cart -v
pytest tests/test_checkout.py::TestCheckout::test_complete_checkout_flow -v

Headless Mode Examples:
━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin::test_valid_login -v --headless
pytest tests/test_products.py::TestProducts::test_add_product_to_cart -v --headless

With Custom Reports (Normal):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin::test_valid_login -v --html=reports/valid_login.html --self-contained-html

With Custom Reports (Headless):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py::TestLogin::test_valid_login -v --headless --html=reports/valid_login.html --self-contained-html


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 📊 VIEW REPORTS                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘

View HTML Report (Default):
━━━━━━━━━━━━━━━━━━━━━━━━━━
start reports\report.html

View HTML Report (Custom):
━━━━━━━━━━━━━━━━━━━━━━━━━
start reports\login.html

View Allure Report:
━━━━━━━━━━━━━━━━━━━
allure serve reports\allure-results
or
allure serve allure-results


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 🚀 ADVANCED OPTIONS                                                                 │
└─────────────────────────────────────────────────────────────────────────────────────┘

Parallel Execution:
━━━━━━━━━━━━━━━━━━
pytest tests/ -v -n 4                     # Normal mode, 4 workers
pytest tests/ -v --headless -n 4          # Headless mode, 4 workers

Stop on First Failure:
━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v -x                       # Normal mode
pytest tests/ -v --headless -x            # Headless mode

Show Print Statements (Debug):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v -s                       # Normal mode only

Run Last Failed Tests:
━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --lf                     # Normal mode
pytest tests/ -v --headless --lf          # Headless mode

Filter by Keyword:
━━━━━━━━━━━━━━━━━
pytest tests/ -v -k "login"               # Normal mode
pytest tests/ -v --headless -k "cart"     # Headless mode


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 💡 COMMON SCENARIOS                                                                 │
└─────────────────────────────────────────────────────────────────────────────────────┘

Quick Smoke Test (Login tests only):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_login.py -v --headless

Debug Single Failing Test:
━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/test_checkout.py::TestCheckout::test_complete_checkout_flow -v -s

Full Regression (Parallel, Headless):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --headless -n 4

CI/CD Pipeline (Headless, Stop on Fail):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pytest tests/ -v --headless -x


╔════════════════════════════════════════════════════════════════════════════════════╗
║                        COPY-PASTE READY COMMANDS                                   ║
╚════════════════════════════════════════════════════════════════════════════════════╝

1. All tests, normal mode:
   pytest tests/ -v

2. All tests, headless mode:
   pytest tests/ -v --headless

3. Specific test file, normal mode:
   pytest tests/test_login.py -v

4. Specific test file, headless mode:
   pytest tests/test_login.py -v --headless

5. Specific test case, normal mode:
   pytest tests/test_login.py::TestLogin::test_valid_login -v

6. Specific test case, headless mode:
   pytest tests/test_login.py::TestLogin::test_valid_login -v --headless

7. Debug mode (browser visible, see output):
   pytest tests/test_login.py::TestLogin::test_valid_login -v -s

8. View Allure report:
   allure serve reports\allure-results


╔════════════════════════════════════════════════════════════════════════════════════╗
║                           AVAILABLE TEST FILES                                     ║
╚════════════════════════════════════════════════════════════════════════════════════╝

test_login.py          → 5 tests  (Login functionality)
test_products.py       → 7 tests  (Product listing and sorting)
test_cart.py           → 6 tests  (Shopping cart operations)
test_checkout.py       → 7 tests  (Checkout process)
                         ────────
                         25 tests total


╔════════════════════════════════════════════════════════════════════════════════════╗
║                           COMMAND FLAGS REFERENCE                                  ║
╚════════════════════════════════════════════════════════════════════════════════════╝

-v              Verbose output
-vv             Extra verbose
-s              Show print statements
-x              Stop on first failure
-n 4            Run with 4 parallel workers
--lf            Run last failed tests only
--ff            Run failed tests first
-k "keyword"    Run tests matching keyword
-l              Show local variables on failure
--headless      Run in headless mode (browser hidden)

═══════════════════════════════════════════════════════════════════════════════════════
